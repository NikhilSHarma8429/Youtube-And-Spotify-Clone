To run the Youtube Downloader Server locally, follow these steps:

	1. Install the required dependencies using npm install

	2. Start the server using npm start

	3. Your Downloading server is hosted at http://localhost:8000 now you can use the download button in YouTube Interface